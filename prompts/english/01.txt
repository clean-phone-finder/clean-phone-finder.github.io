Create a self-contained HTML file in English with embedded CSS and JavaScript, without using any CSS frameworks, JavaScript frameworks, libraries, or CDNs. The page title should be Prompt generator: Find smartphone without annoying features.

The purpose of the page is to help the user generate a high-quality text prompt to select a smartphone for everyday use without intrusive ads, spyware, or adware that can aggressively install apps without the ability to stop it, while also considering real hardware issues such as overheating, throttling, battery problems, and component stability, and software issues such as freezes, crashes, unstable updates, firmware bugs, and performance degradation over time, based on real user reviews and discussions.

The page must be fully static, responsive, with minimalist CSS, and support light and dark themes with a toggle. The theme toggle must be placed at the top of the page. All interface text, placeholders, SEO, and the generated prompt must be in English.

The form must include an optional input field for entering a smartphone name with a datalist of popular models, which will be filled later. There should be an optional input field for selecting a country with a datalist of countries, which will also be filled later. There should be an optional input field for selecting a response language with a datalist of languages, which will be filled later. All datalists must be dynamically populated from lists in JavaScript, like settings. All fields can be empty or filled independently.

There must be a read-only textarea from which the generated result can be copied, and a separate Copy button. The textarea value must automatically update whenever the smartphone name, country, or language changes.

The logic for generating the textarea text is as follows. If smartphone name, country, and language are missing, a general English prompt for worldwide smartphone selection is generated based on real user discussions and reviews, with Reddit as a mandatory source. This prompt must clearly warn the user if a model is less than one year old and therefore lacks independent reviews, and suggest alternative models without the described issues.

If only a country is entered, the prompt must consider regional characteristics of that country, model availability, regional firmware, restrictions, and specific user complaints from that region, while Reddit remains a mandatory source.

If only a smartphone name is entered, the prompt must focus on analyzing that specific model, checking hardware and software issues, user complaints about ads, spyware, adware, forced app installations, regional firmware differences, and time on the market.

If both smartphone name and country are entered, the prompt must consider both parameters without ignoring either.

If a language is entered, regardless of other fields, the prompt must clearly instruct the AI assistant to respond in that language. If no language is specified, the response should be in English.

In every case, the prompt generated by this HTML page must strictly forbid the AI from fabricating facts, making assumptions without sources, or providing unverified information. The prompt must rely only on real user reports, discussions, and public sources, explicitly indicating Reddit as a mandatory but not sole source. Additionally, the prompt generated by this HTML page must be continuous text without emojis, HTML, Markdown, or any specialized formatting, and must be broken into logical paragraphs with vertical spacing for human readability.

Below the page, there must be links to free AI assistants ChatGPT, Gemini, Copilot, and Claude, each with an img icon and name. The icons must use local files with these paths: ./assets/images/openai-chatgpt-color-icon.svg, ./assets/images/google-gemini-color-icon.svg, ./assets/images/microsoft-copilot-color-icon.svg, ./assets/images/anthropic-claude-color-icon.svg. Do not generate or modify the images themselves.

Next, there must be placeholder links to already published or upcoming posts and discussions that directly describe and discuss this HTML page being generated. Additionally, at the bottom of the page, there must be a link to the source code GitHub repository.

The head must contain only basic SEO tags, including title and meta description, without advanced SEO or structured data.

The result must be a fully ready HTML file without explanations, comments, or any text outside the code.